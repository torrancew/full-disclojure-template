(ns {{name}}.client-test
  (:require [{{name}}.client :as client])
  (:require-macros [cljs.test :refer [async deftest is testing]]))

(deftest some-test
  (testing "FIXME: Some Test"
    (is (= 0 1))))
